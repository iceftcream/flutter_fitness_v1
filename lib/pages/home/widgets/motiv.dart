import 'package:flutter/material.dart';

class Motiv extends StatelessWidget {
  const Motiv({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
